<h1> new of favourite dish</h1>
<%= form_for @favourite_dish do |f| %>
   <% if @favourite_dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@favourite_dish.errors.count, "error") %> prohibited this article from being saved:</h2>
      <ul>
        <% @favourite_dish.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  Add item to your favourite
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :dish_id, value: @dish.id %>
  <%= f.submit  %>
<% end %>
<br>
<%= link_to "Back To Home Page", homepage_home_path %>
